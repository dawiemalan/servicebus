micronaut:
  security.enabled: false
  application:
    name: servicebus-controller
  config-client:
    enabled: true
kubernetes:
  client:
    discovery:
      mode: endpoint
      mode-configuration:
        endpoint:
          watch:
            enabled: true
    config-maps:
      enabled: true
      use-api: true
