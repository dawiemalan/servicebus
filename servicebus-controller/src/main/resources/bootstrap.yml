micronaut:
  security.enabled: false
  application:
    name: demo
  config-client:
    enabled: true
kubernetes:
  client:
    discovery:
      mode: endpoint
      mode-configuration:
        endpoint:
          watch:
            enabled: true
